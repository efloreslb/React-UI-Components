import React from 'react';
import './Display.css';

const CalculatorDisplay = (props) => {
    return (
        <div>
            {props.display}
        </div>
    )
}

export default CalculatorDisplay;
